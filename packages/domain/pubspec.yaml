name: dopa_domain
description: Pure Dart domain contracts and policies for the Dopa app.
version: 0.1.0
publish_to: none

environment:
  sdk: '>=3.13.0 <3.14.0'

resolution: workspace

dev_dependencies:
  test: ^1.25.0
